No errors now.I just now tested the app with a video, where I gave driver id as DRV_002 and Trip_002, but in history analytics it is only showing data of driver id DRV_001.Also while testing for the video, alerts where fired, but its not displayed under Alert Intelligence. It's showing:
Powered by REALTIME_ALERTS — full fleet alert log with timestamps, severity, and context.
No alerts fired yet across all sessions


SELECT DRIVER_ID, TRIP_ID, ALERT_TYPE, SEVERITY, CREATED_AT_CLIENT
FROM REALTIME_ALERTS
WHERE DRIVER_ID = 'DRV_002'
ORDER BY CREATED_AT_CLIENT DESC
LIMIT 50;

SELECT DRIVER_ID, TRIP_ID, SESSION_ID, CREATED_AT_CLIENT
FROM UNIFIED_DRIVER_SESSION_SUMMARY
WHERE DRIVER_ID = 'DRV_002'
ORDER BY CREATED_AT_CLIENT DESC
LIMIT 50;

SELECT *
FROM DRIVER_RISK_LEADERBOARD
WHERE DRIVER_ID = 'DRV_002';

REALTIME_ALERTSColumns present in it:ALERT_ID
SESSION_ID
DRIVER_ID
TRIP_ID
ALERT_TIMESTAMP_SECONDS
ALERT_WALL_TIME
ALERT_TYPE
SEVERITY
SEVERITY_SCORE
ESCALATION_LEVEL
FATIGUE_SCORE
OFFROAD_PROB
ZONE_AT_ALERT
CONFIDENCE
MESSAGE
CREATED_AT
CREATED_AT_CLIENT
LIGHTING_AT_ALERT

SELECT DRIVER_ID, TRIP_ID, SESSION_ID, ALERT_TYPE, CREATED_AT_CLIENT
FROM REALTIME_ALERTS
ORDER BY COALESCE(CREATED_AT_CLIENT, CREATED_AT) DESC
LIMIT 50;

SELECT DRIVER_ID, TRIP_ID, SESSION_ID
FROM UNIFIED_DRIVER_SESSION_SUMMARY
ORDER BY SESSION_ID DESC
LIMIT 50;

DESC TABLE UNIFIED_DRIVER_SESSION_SUMMARY;

